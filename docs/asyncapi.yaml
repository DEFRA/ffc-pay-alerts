asyncapi: 2.2.0
info:
  title: FFC Pay Alert
  version: '1.0.0'
channels:
  alert:
    subscribe:
      message:
        $ref: '#/components/messages/SendRequest'
components:
  messages:
    SendRequest:
      contentType: application/json
      payload:
        type: object
        properties:
          test:
            type: string
            description: Placeholder property